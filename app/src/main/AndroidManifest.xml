<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eus.ehu.helpappandroid.helpappandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BilbaoActivity"
            android:label="@string/activity_bilbao" />
        <activity
            android:name=".AccidentActivity"
            android:label="@string/activity_accident" />
        <activity
            android:name=".GoodsActivity"
            android:label="@string/activity_goods" />
        <activity
            android:name=".HealthActivity"
            android:label="@string/activity_health" />
        <activity
            android:name=".ViolenceActivity"
            android:label="@string/activity_violence" />
        <activity
            android:name=".FireActivity"
            android:label="@string/activity_fire" />
        <activity android:name=".ContentActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".RecordsActivity"></activity>
    </application>

</manifest>